# API reference

TypeScript API reference is generated by TypeDoc into this section at build
time. When built standalone, the in-repo API fixture is rendered here.

Contract specifications (OpenAPI / AsyncAPI) are linked from the relevant
service pages; TypeDoc documents code APIs, not wire contracts.
