# Architecture

Component diagram and data flow for the example service. Real per-service
TechDocs Markdown is sourced from the service's workspace mirror at build time —
never seeded into the service code repo (repo-boundary rule).
